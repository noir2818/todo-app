window.TODO_APP_CONFIG = {
  // Set this to your deployed Cloudflare Pages domain before building if AI chat
  // should work inside the Android app, for example:
  // apiBaseUrl: "https://your-project.pages.dev"
  apiBaseUrl: ""
};
